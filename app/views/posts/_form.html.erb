<%= form_for(post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div cols="80" rows="20">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <% if params[:forum] %><input type="hidden" id="forum_id" name="post[forum_id]" value="<%= params[:forum] %>" /><% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
