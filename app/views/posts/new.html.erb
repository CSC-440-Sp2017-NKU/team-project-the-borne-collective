<div class="container-fluid">
  <h1><%= Course.find(@post.course_id).name %></h1>
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form', post: @post %>
  </div>
</div>
<br><br><%= link_to 'Back', "/courses/#{params[:course]}" %>
